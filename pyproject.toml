[project]
name = "hkmj"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
    "hypothesis>=6.156.9",
    "pytest>=9.1.1",
    "ruff>=0.15.22",
    "ty>=0.0.61",
]
