[project]
name = "hkmj-hand-v1"
version = "0.1.0"
description = "A seeded four-agent Verifiers environment for Hong Kong mahjong hands"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hkmj-core",
    "verifiers",
]

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
