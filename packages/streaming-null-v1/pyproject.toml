[project]
name = "hkmj-streaming-null-v1"
version = "0.1.0"
description = "A one-turn streaming Verifiers harness without tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "verifiers",
]

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
